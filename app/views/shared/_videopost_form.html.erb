<%= form_for(@videopost, remote: true) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Share a YouTube video..." %>
    </div>
    <%= f.submit "Share", class: "btn btn-large btn-primary" %>
<% end %>